export default class FormButton {
    constructor(icon, action, onlyIfExists = false) {
        this.icon = icon;
        this.action = action;
        this.onlyIfExists = onlyIfExists;
    }
}
//# sourceMappingURL=form-button.js.map